package cc.fascinated.core.module;

public enum ModulePriority {
    /**
     * Highest will run first
     * Normal will run as it's registered
     * Low will run last
     */
    HIGHEST, NORMAL, LOW
}
